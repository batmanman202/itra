#!/bin/bash

LOG_DIR="/var/log"
ERROR_PATTERN="error"

find "${LOG_DIR}" -type f -name "*.log" -exec grep -l "${ERROR_PATTERN}" {} \;
if [ $? -ne 0 ]; then
	echo "Не найдено файлов в директории ${LOG_DIR} с тестом error"
fi
